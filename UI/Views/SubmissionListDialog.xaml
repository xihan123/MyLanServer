<Window x:Class="MyLanServer.UI.Views.SubmissionListDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:MyLanServer.UI.Converters"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Background="{DynamicResource MaterialDesignPaper}"
        Title="提交人列表" Height="700" Width="1100"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        AllowsTransparency="False"
        ResizeMode="CanResizeWithGrip">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="46" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0,0,0,1"
                      UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- 转换器资源 -->
        <local:UtcToLocalConverter x:Key="UtcToLocalConverter" />

        <!-- 窗口控制按钮样式 -->
        <Style x:Key="WindowControlButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        </Style>

        <!-- 右键菜单 -->
        <ContextMenu x:Key="SubmissionContextMenu">
            <MenuItem Header="打开文件夹并选中文件"
                      Command="{Binding Path=PlacementTarget.Tag.OpenFileAndSelectCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="FolderOpen" />
                </MenuItem.Icon>
            </MenuItem>

            <Separator />

            <MenuItem Header="删除提交记录"
                      Command="{Binding Path=PlacementTarget.Tag.DeleteSubmissionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="Delete" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </Window.Resources>

    <materialDesign:Card UniformCornerRadius="12" Margin="0" materialDesign:ElevationAssist.Elevation="Dp4"
                         Background="{DynamicResource MaterialDesignPaper}" BorderThickness="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Grid Grid.Row="0" Height="46" Background="Transparent">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <materialDesign:PackIcon Kind="AccountMultiple" VerticalAlignment="Center" Margin="0,0,8,0"
                                             Foreground="{DynamicResource MaterialDesignPrimary}" />
                    <TextBlock Text="提交人列表" VerticalAlignment="Center" FontWeight="Medium" />
                    <TextBlock Text=" - " VerticalAlignment="Center" Margin="4,0,0,0" />
                    <TextBlock Text="{Binding TaskTitle}" VerticalAlignment="Center" FontWeight="Medium" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button Style="{StaticResource WindowControlButtonStyle}" Click="CloseButton_Click" ToolTip="关闭">
                        <materialDesign:PackIcon Kind="WindowClose" />
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 搜索栏 -->
            <Border Grid.Row="1" Margin="16,8,16,8" CornerRadius="8" Padding="16"
                    Background="{DynamicResource MaterialDesignCardBackground}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                        <materialDesign:PackIcon Kind="Magnify" VerticalAlignment="Center" Margin="0,0,8,0"
                                                 Foreground="{DynamicResource MaterialDesignBody}" />
                        <TextBlock Text="搜索" VerticalAlignment="Center" FontWeight="Medium" />
                    </StackPanel>

                    <TextBox Grid.Column="1"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="搜索姓名、联系方式、部门..."
                             Padding="8,6"
                             VerticalAlignment="Center" />

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <TextBlock Text="共 " VerticalAlignment="Center" />
                        <TextBlock Text="{Binding TotalCount}" VerticalAlignment="Center" FontWeight="Bold" />
                        <TextBlock Text=" 条记录" VerticalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 数据列表 -->
            <Border Grid.Row="2" Margin="16,0,16,8" CornerRadius="8" BorderThickness="1"
                    BorderBrush="{DynamicResource MaterialDesignDivider}">
                <DataGrid ItemsSource="{Binding FilteredSubmissions}"
                          EnableRowVirtualization="False"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          HeadersVisibility="All"
                          GridLinesVisibility="Horizontal"
                          CanUserSortColumns="True"
                          Background="Transparent"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          BorderThickness="0">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
                            <Setter Property="Padding" Value="8,6" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                            <Setter Property="ContextMenu" Value="{StaticResource SubmissionContextMenu}" />
                            <Setter Property="Tag"
                                    Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}" />
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <!-- 姓名 -->
                        <DataGridTextColumn Header="姓名" Binding="{Binding SubmitterName}" Width="100"
                                            FontWeight="SemiBold" />

                        <!-- 联系方式 -->
                        <DataGridTextColumn Header="联系方式" Binding="{Binding Contact}" Width="120" />

                        <!-- 部门 -->
                        <DataGridTextColumn Header="部门" Binding="{Binding Department}" Width="120" />

                        <!-- 提交时间 -->
                        <DataGridTextColumn Header="提交时间"
                                            Binding="{Binding Timestamp, Converter={StaticResource UtcToLocalConverter}, StringFormat='yyyy-MM-dd HH:mm'}"
                                            Width="140" />

                        <!-- 文件名 -->
                        <DataGridTextColumn Header="文件名" Binding="{Binding OriginalFilename}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                    <Setter Property="ToolTip" Value="{Binding OriginalFilename}" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- IP地址 -->
                        <DataGridTextColumn Header="IP地址" Binding="{Binding ClientIp}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- 底部按钮 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,0,16,16">
                <Button Command="{Binding ClearAllSubmissionsCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Width="120" Height="36"
                        Margin="0,0,12,0"
                        Background="#FFE0E0"
                        Foreground="#D32F2F"
                        BorderBrush="#D32F2F">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="DeleteSweep" Margin="0,0,6,0" VerticalAlignment="Center" />
                        <TextBlock Text="清空全部" />
                    </StackPanel>
                </Button>

                <Button Command="{Binding CloseCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Width="100" Height="36">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Close" Margin="0,0,6,0" VerticalAlignment="Center" />
                        <TextBlock Text="关闭" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>