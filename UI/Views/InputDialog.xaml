<Window x:Class="MyLanServer.UI.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="输入"
        SizeToContent="WidthAndHeight"
        MinWidth="400"
        MaxWidth="800"
        MaxHeight="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <materialDesign:Card UniformCornerRadius="12" Margin="5"
                         materialDesign:ElevationAssist.Elevation="Dp4"
                         Background="{DynamicResource MaterialDesignPaper}"
                         BorderThickness="0">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <materialDesign:PackIcon Kind="Edit" Width="28" Height="28"
                                         Foreground="{DynamicResource MaterialDesignPrimaryBrush}"
                                         VerticalAlignment="Center" Margin="0,0,12,0" />
                <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold"
                           VerticalAlignment="Center" />
            </StackPanel>

            <!-- 提示文本 -->
            <TextBlock Grid.Row="1"
                       Text="{Binding Prompt}"
                       TextWrapping="Wrap"
                       FontSize="14"
                       Margin="0,0,0,12"
                       Foreground="{DynamicResource MaterialDesignBody}" />

            <!-- 输入框 -->
            <TextBox Grid.Row="2"
                     x:Name="InputTextBox"
                     Text="{Binding InputValue, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Padding="8,6"
                     materialDesign:HintAssist.Hint="{Binding Placeholder}"
                     Margin="0,0,0,16"
                     VerticalAlignment="Center" />

            <!-- 按钮区域 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Width="80"
                        Height="36"
                        Margin="0,0,12,0"
                        Click="Cancel_Click"
                        IsCancel="True" />
                <Button Content="确定"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Width="80"
                        Height="36"
                        Click="Ok_Click"
                        IsDefault="True" />
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>