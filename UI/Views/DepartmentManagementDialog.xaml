<Window x:Class="MyLanServer.UI.Views.DepartmentManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Background="{DynamicResource MaterialDesignPaper}"
        Title="部门管理" Height="600" Width="900" MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        AllowsTransparency="False"
        ResizeMode="CanResizeWithGrip">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="46" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0,0,0,1"
                      UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- 窗口控制按钮样式 -->
        <Style x:Key="WindowControlButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        </Style>

        <!-- 右键菜单 -->
        <ContextMenu x:Key="DepartmentContextMenu" Style="{StaticResource MaterialDesignContextMenu}">
            <MenuItem Header="编辑部门"
                      Command="{Binding Path=PlacementTarget.Tag.EditDepartmentCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="Pencil" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Header="删除部门"
                      Command="{Binding Path=PlacementTarget.Tag.DeleteDepartmentCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="Delete" />
                </MenuItem.Icon>
            </MenuItem>

            <Separator />

            <MenuItem Header="置顶"
                      Command="{Binding Path=PlacementTarget.Tag.MoveToTopCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="ArrowUpBold" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Header="上移"
                      Command="{Binding Path=PlacementTarget.Tag.MoveUpCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="ArrowUp" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Header="下移"
                      Command="{Binding Path=PlacementTarget.Tag.MoveDownCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="ArrowDown" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>
    </Window.Resources>

    <materialDesign:Card UniformCornerRadius="12" Margin="0" materialDesign:ElevationAssist.Elevation="Dp4"
                         Background="{DynamicResource MaterialDesignPaper}" BorderThickness="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Grid Grid.Row="0" Height="46" Background="Transparent">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <materialDesign:PackIcon Kind="Domain" VerticalAlignment="Center" Margin="0,0,8,0"
                                             Foreground="{DynamicResource MaterialDesignPrimary}" />
                    <TextBlock Text="部门管理" VerticalAlignment="Center" FontWeight="Medium" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button Style="{StaticResource WindowControlButtonStyle}" Click="CloseButton_Click" ToolTip="关闭">
                        <materialDesign:PackIcon Kind="WindowClose" />
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 内容区域 -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" MinWidth="300" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="320" MinWidth="280" />
                </Grid.ColumnDefinitions>

                <!-- 左侧：部门列表 -->
                <Border Grid.Column="0" CornerRadius="8" BorderBrush="{DynamicResource MaterialDesignOutlineBorder}"
                        BorderThickness="1" Background="{DynamicResource MaterialDesignCardBackground}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- 列表标题 -->
                        <Border Grid.Row="0" Padding="16,12" Background="{DynamicResource MaterialDesignDivider}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FormatListBulleted" VerticalAlignment="Center"
                                                         Margin="0,0,8,0"
                                                         Foreground="{DynamicResource MaterialDesignPrimary}" />
                                <TextBlock Text="部门列表" FontWeight="Medium" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding Departments.Count, StringFormat=' ({0})'}"
                                           VerticalAlignment="Center" Margin="8,0,0,0"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}" />
                            </StackPanel>
                        </Border>

                        <!-- DataGrid -->
                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding FilteredDepartments}"
                                  SelectedItem="{Binding SelectedDepartment}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  SelectionMode="Single"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  materialDesign:DataGridAssist.CellPadding="8">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="排序" Binding="{Binding SortOrder}" Width="60" />
                                <DataGridTextColumn Header="部门名称" Binding="{Binding Name}" Width="*" />
                            </DataGrid.Columns>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                                    <Setter Property="ContextMenu" Value="{StaticResource DepartmentContextMenu}" />
                                    <Setter Property="Tag"
                                            Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}" />
                                    <EventSetter Event="MouseRightButtonDown"
                                                 Handler="DataGridRow_MouseRightButtonDown" />
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- 右侧：操作面板 -->
                <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- 编辑区域 -->
                        <Border CornerRadius="8" BorderBrush="{DynamicResource MaterialDesignOutlineBorder}"
                                BorderThickness="1" Background="{DynamicResource MaterialDesignCardBackground}"
                                Margin="0,0,0,16">
                            <StackPanel Margin="16">
                                <TextBlock
                                    Text="{Binding IsEditMode, Converter={StaticResource BoolToStringConverter}, ConverterParameter='编辑部门|添加部门'}"
                                    FontWeight="Medium" Margin="0,0,0,16" />

                                <TextBlock Text="部门名称" Margin="0,0,0,8" />
                                <TextBox Text="{Binding DepartmentName, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                         materialDesign:HintAssist.Hint="请输入部门名称"
                                         Padding="8,6"
                                         KeyDown="DepartmentName_KeyDown" />

                                <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                    <Button Content="保存"
                                            Command="{Binding SaveDepartmentCommand}"
                                            Style="{StaticResource MaterialDesignRaisedButton}"
                                            Margin="0,0,8,0"
                                            Width="100" />
                                    <Button Content="取消"
                                            Command="{Binding CancelEditCommand}"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"
                                            Width="100" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- 操作按钮 -->
                        <Border CornerRadius="8" BorderBrush="{DynamicResource MaterialDesignOutlineBorder}"
                                BorderThickness="1" Background="{DynamicResource MaterialDesignCardBackground}">
                            <StackPanel Margin="16">
                                <TextBlock Text="操作" FontWeight="Medium" Margin="0,0,0,16" />

                                <!-- 搜索框 -->
                                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                         materialDesign:HintAssist.Hint="搜索部门..."
                                         materialDesign:TextFieldAssist.HasClearButton="True"
                                         Padding="8,6"
                                         Margin="0,0,0,16">
                                    <TextBox.InputBindings>
                                        <KeyBinding Key="Escape" Command="{Binding ClearSearchCommand}" />
                                    </TextBox.InputBindings>
                                </TextBox>

                                <Button Command="{Binding AddDepartmentCommand}"
                                        Style="{StaticResource MaterialDesignRaisedButton}"
                                        Margin="0,0,0,8"
                                        HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Plus" Margin="0,0,8,0" />
                                        <TextBlock Text="添加部门" />
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding EditDepartmentCommand}"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Margin="0,0,0,8"
                                        HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Pencil" Margin="0,0,8,0" />
                                        <TextBlock Text="编辑部门" />
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding DeleteDepartmentCommand}"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Margin="0,0,0,8"
                                        HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Delete" Margin="0,0,8,0" />
                                        <TextBlock Text="删除部门" />
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding ClearAllDepartmentsCommand}"
                                        Style="{StaticResource MaterialDesignRaisedButton}"
                                        Background="#FFE5E5E5"
                                        Foreground="#FFD32F2F"
                                        BorderBrush="#FFD32F2F"
                                        Margin="0,0,0,8"
                                        HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="DeleteForever" Margin="0,0,8,0" />
                                        <TextBlock Text="清空全部部门" />
                                    </StackPanel>
                                </Button>

                                <Separator Margin="0,8,0,8" />

                                <Button Command="{Binding MoveToTopCommand}"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Margin="0,0,0,8"
                                        HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ArrowUpBold" Margin="0,0,8,0" />
                                        <TextBlock Text="置顶" />
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding MoveUpCommand}"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Margin="0,0,0,8"
                                        HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ArrowUp" Margin="0,0,8,0" />
                                        <TextBlock Text="上移" />
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding MoveDownCommand}"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Margin="0,0,0,8"
                                        HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ArrowDown" Margin="0,0,8,0" />
                                        <TextBlock Text="下移" />
                                    </StackPanel>
                                </Button>

                                <Separator Margin="0,8,0,8" />

                                <Button Command="{Binding ImportDepartmentsCommand}"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Margin="0,0,0,8"
                                        HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Import" Margin="0,0,8,0" />
                                        <TextBlock Text="导入部门" />
                                    </StackPanel>
                                </Button>

                                <Button
                                    Command="{Binding ExportDepartmentsCommand}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    HorizontalContentAlignment="Left">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Export" Margin="0,0,8,0" />
                                        <TextBlock Text="导出部门" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- 底部按钮 -->
            <Border Grid.Row="2" Padding="16,12" Background="{DynamicResource MaterialDesignDivider}">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="关闭"
                            Command="{Binding CloseDialogCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Width="100" />
                </StackPanel>
            </Border>
        </Grid>
    </materialDesign:Card>
</Window>